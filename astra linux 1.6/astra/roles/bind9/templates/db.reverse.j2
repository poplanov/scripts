$TTL 604800
@ IN SOA {{ ansible_fqdn }}. root.{{ ansible_domain }}. (
	{{ lookup('pipe','date +%Y%m%d') }}01 ; Serial
	604800 ; Refresh
	86400 ; Retry
	2419200 ; Expire
	604800 ) ; Negative Cache TTL
	IN				NS	{{ ansible_fqdn }}.
{{ ansible_host.split('.')[3] }}	IN	PTR	{{ ansible_fqdn }}.
